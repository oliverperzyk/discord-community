/**
 * @summary Base cache service class.
 * @description A base class for all cache services.
 */
abstract class BaseCacheService {
    /**
     * @summary Protected constructor.
     * @description Protected constructor to prevent instantiation, while allowing inheritance.
     */
    protected constructor() {}
}

export { BaseCacheService }
