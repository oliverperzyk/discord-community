/**
 * @summary Options for the translate button component.
 * @description Options for the translate button component. It gets message identifier from query part from custom identifier.
 */
interface ITranslateButtonComponentOptions {
    /**
     * @summary The message identifier.
     * @description The message identifier to translate.
     */
    readonly m: string
}

export type { ITranslateButtonComponentOptions }
