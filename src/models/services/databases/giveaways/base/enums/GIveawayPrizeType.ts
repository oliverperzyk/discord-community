/**
 * @summary The prize type enum for the giveaways.
 * @description This enum is used to store the prize type for the giveaways.
 */
const enum GiveawayPrizeType {
    /**
     * @summary A role on Discord.
     */
    ROLE = "ROLE",
    /**
     * @summary Other prize type.
     */
    OTHER = "OTHER",
}

export { GiveawayPrizeType }
