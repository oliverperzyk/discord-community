/**
 * @summary The language enum for the testing things translations.
 * @description This enum is used to store the language for the testing things translations.
 */
const enum TestingThingsTranslationsLanguage {
    /**
     * @summary The Polish language.
     */
    POLISH = "PL",
    /**
     * @summary The English language.
     * @description There is no need to split the English language by dialects, as it's not necessary for this type of project.
     */
    ENGLISH = "EN",
}

export { TestingThingsTranslationsLanguage }
